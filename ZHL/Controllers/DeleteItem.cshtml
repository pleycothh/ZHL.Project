@page "/api/deleteItem/{cacheId}/{hashId}/"

@using ZHL.Pages
@model IndexModel
@{
    Model._itemProvider.DeleteItem(
    RouteData.Values["cacheId"] as string,
    RouteData.Values["hashId"] as string
        );
}
