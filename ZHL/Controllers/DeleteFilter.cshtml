@page "/api/deleteFilter/{cacheId}/{hashId}/"

@model ZHL.GUI.Pages.FilterModel
@{
    Model._filterProvider.DeleteFilter(
        RouteData.Values["cacheId"] as string,
        RouteData.Values["hashId"] as string
    );
}
